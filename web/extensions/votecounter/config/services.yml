services:
  mafiascum.votecounter.listener:
    class: mafiascum\votecounter\event\main_listener
    arguments:
      - "@template"
      - "@user"
      - "@dbal.conn"
      - "%core.table_prefix%"
      - "@auth"
    tags:
      - { name: event.listener }
