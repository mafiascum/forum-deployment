services:
  mafiascum.banners.listener:
    class: mafiascum\banners\event\main_listener
    arguments:
      - "@dbal.conn"
      - "@config"
      - "@template"
      - "%core.table_prefix%"
    tags:
      - { name: event.listener }
